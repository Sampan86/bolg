define(function(require) {
    "use strict";
    var system = require('../js/vendor/system');
    if (system.userSystem.mobile) {
    	var $ = require('../js/vendor/jquery');
    }else {
    	
    };


});
